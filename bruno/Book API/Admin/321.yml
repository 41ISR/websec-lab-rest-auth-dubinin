info:
  name: "321"
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/admin/users
  headers:
    - name: Authorization
      value: Bearer ADMIN_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
